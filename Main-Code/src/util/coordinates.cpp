#include "main.h"

#include <iostream>
#include <tuple>
#include <vector>

namespace Coordinates
{
    std::vector<int> processMovement(float x, float y, float theta, AutonPosition &position)
    {
        std::vector<int> test = {1, 2, 3};
        return test;
    }
}